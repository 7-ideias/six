import 'package:flutter/material.dart';

class FinanceiroTela extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('Financeiro'),
        ),
        body: Container()
    );
  }
}