import 'package:flutter/material.dart';

class AjudaESuporteTela extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('Ajuda e Suporte'),
        ),
        body: Container()
    );
  }
}