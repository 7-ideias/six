import 'package:flutter/material.dart';

class ResumoTela extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('Resumo'),
        ),
        body: Container()
    );
  }
}